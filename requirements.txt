# Backend
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.9
opencv-python==4.10.0.84
numpy==1.26.4

# Fire detector (Keras/TensorFlow)
tensorflow==2.17.0

# People detector (YOLO)
ultralytics==8.3.0

# AI-generated detector (PyTorch, CUDA 12.1 build — matches the `ml` conda env)
torch==2.5.1
torchvision==0.20.1
pillow==10.4.0
transformers==4.46.0  # required for AI_DETECTOR.py's HuggingFace pipeline

# Frontend
streamlit==1.39.0

# Tests
pytest==8.3.3
httpx==0.27.2  # required by fastapi.testclient.TestClient (used in test_main.py)

# NOTE: torch/torchvision here are the generic PyPI wheels (CPU-only on some
# platforms). If `torch.cuda.is_available()` returns False after installing
# from this file, reinstall with the CUDA 12.1 index instead:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121

